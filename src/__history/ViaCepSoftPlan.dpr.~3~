program ViaCepSoftPlan;

uses
  System.StartUpCopy,
  FMX.Forms,
  untPrincipal in 'untPrincipal.pas' {frmPrincipal},
  untLogin in 'untLogin.pas' {Ti};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TTi, Ti);
  Application.Run;
end.
